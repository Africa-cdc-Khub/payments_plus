# Disable directory browsing
Options -Indexes

# Deny access to all files
<Files "*">
    Require all denied
</Files>

# Prevent access to all file types
<FilesMatch ".*">
    Require all denied
</FilesMatch>
